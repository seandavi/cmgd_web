# Generic single-database configuration.

Run this from the top-level directory where `alembic.ini` is
located.

## Generate a new revision

``` sh
poetry run alembic revision --autogenerate -m "base models"
```

## And upgrade the database

``` sh
dotenv poetry run alembic upgrade head
```
